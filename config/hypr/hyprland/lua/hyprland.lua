-- Hyprland Lua Config (v0.55+)
-- Modular config: requires separate .lua files
-- NOTE: Requires Hyprland 0.55 or later (Lua config not available in 0.54.x)

require("colors")   -- colors, hyprbars
require("env")      -- environment variables
require("execs")    -- autostart exec-once
require("general")  -- general, decoration, animations, gestures, plugins
require("layouts")  -- dwindle, master, scrolling layouts
require("rules")    -- window rules, layer rules, workspace rules
require("keybinds") -- all keybinds
